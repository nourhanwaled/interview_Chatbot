# Copy this file to secrets.toml and fill in keys for the provider you use.
# Default provider is Groq (see LLM_PROVIDER in config.py).

# Free tier: https://console.groq.com/keys
GROQ_API_KEY = "gsk_..."

# Only needed when LLM_PROVIDER = "openai" in config.py
OPEN_API_KEY = "sk-..."
